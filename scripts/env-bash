#!/bin/bash
#!/bin/bash
# This script creates a one line env bash script for answering the first few tasks
# use this command like this
# ../scripts/bash_that FILENAME ANSWER_TO_TASK
echo "#!/bin/env bash" >> $1
echo "$2" >> $1
chmod u+x $1

